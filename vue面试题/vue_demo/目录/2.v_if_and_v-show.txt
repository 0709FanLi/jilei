v-show和v-if指令的共同点和不同点？

共同点：都能控制元素的显示和隐藏
不同点：v-show 使用 css display:none 控制隐藏，dom树只会编译一次；
       v-if   是动态的向dom树添加或删除元素，若初始值为false就不会编译，
              v-if不停创建和销毁元素比较耗费性能
总结：如果要频繁切换某点，使用v-show(切换开销比较小，初始值比较大)。如果不需要
     频繁切换某点使用v-if(初始渲染开销较小，切换开销比较大)              